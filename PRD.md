# PRD · InkLine（文章转短视频素材流水线）

| 项目 | 内容 |
|---|---|
| 名称 | InkLine（墨线） |
| 版本 | v0.1.0（MVP） |
| 状态 | 已发布 |
| 目标用户 | 个人知识类短视频创作者（小红书 / 视频号 / 抖音 / B 站） |

## 1. 背景与问题

创作者做一篇「文章 → 短视频」的内容，素材生产环节高度重复：

1. 看到一篇好文章，想提炼分享
2. 分析文章结构、提炼观点，决定哪些段落值得配图（分镜）
3. 逐张产出配图，风格要统一——**这是全流程的关键环节**
4. 逐镜头写口播旁白
5. 写标题（要勾子）、正文、标签、置顶评论
6. 录视频、剪辑合成、发布

其中 2–5 步是纯素材生产，单篇耗时 2–4 小时，且质量依赖临场状态。这些环节可以被「AI 起草 + 人工确认」大幅压缩。

## 2. 产品定位

**一句话**：本地运行的文章转短视频流水线——文章进，短视频所需的全部素材出（分镜、插画、旁白、发布文案）；远期逐步纳入字幕、音频、视频生成与合并，最终输出完整成片，交给第三方剪辑软件做最后精修。

**初期满足的主链路**：

```
好文章（自行输入 / 提供网址[二期]）→ 提炼观点 → 画配图（关键）→ 写旁白（即后期字幕）→ 写标题文案 → 录视频（用户用第三方工具）→ 发布
```

**边界**：

- 录音、剪辑精修、发布由用户用第三方工具（剪映等）完成——音色选择与节奏判断的质量高于自动化价值
- 不做自动发布——各平台无开放 API，硬自动化有风控风险
- 不做云端多用户——单人本地工具，数据不出本机

## 3. 用户旅程

```
粘贴文章 ──► 提炼观点·生成分镜 ──► 修订分镜 ──► 生成配图 ──► 生成旁白 ──► 生成发布包 ──► 复制素材到剪辑工具
                (AI)               (人工)       (AI+人工)     (AI+人工)      (AI)           (人工)
```

核心设计原则：**每个 AI 环节后面都跟一个人工确认点**。AI 负责把 60 分的草稿快速堆出来，人负责把它改到 90 分；任何一步不满意都可以单独重跑，不牵连其它环节。

## 4. 功能需求

### 4.1 项目管理

- 新建项目：输入文章标题 + 正文（URL 自动抓取导入在 v0.2）
- 项目列表：展示标题、创建时间、所处阶段（draft / shots / images / voiceover / package）
- 删除项目（连同其图片数据）

### 4.2 提炼观点 + 分镜（Shot List）生成

**输入**：文章标题 + 正文
**输出**：结构化分镜数组，每条含：

| 字段 | 说明 |
|---|---|
| `position` | 放在哪一段之后（如「第 1 段后」「论点三段后」） |
| `theme` | 这张图的主题，一句话 |
| `core_idea` | 核心意思，口语化中文 |
| `structure_type` | 前后对比 / 概念隐喻 / 闭环循环 / 过程分解 |
| `xiaohei_action` | 画面主角执行的核心动作（必须承担概念，不能装饰） |
| `labels` | 建议印在图上的中文标注词，3–6 个，每个 ≤8 字 |
| `elements` | 建议出现的视觉元素，4–6 个 |

**生成规则（写入系统提示词）**：

- 1000–1500 字文章配 4–6 张图，只打认知锚点，不平均铺
- 跳过一句话转折段和纯情绪口号结尾——配图会掐断节奏
- 每张图只讲一个核心结构
- 画面必须有明确主角执行核心概念动作

**人工修订**：所有字段可直接编辑后保存，保存后才进入配图环节。

### 4.3 配图生成（关键环节）

- 按分镜逐张生成，或一键批量生成
- **默认 IP**：小黑——实心黑色小怪（白点眼、细腿、面无表情），风格 DNA 为纯白背景、极简黑色手绘线稿、略抖笔触、大量留白；红橙蓝三色标注分工（红=问题警告、橙=主路径强调、蓝=补充说明）
- **IP 与风格可定制**：小黑只是缺省 IP；用户后续可创建自己的 IP 角色（v0.2），风格 DNA 在设置页可即时自定义，并可选用风格预设——科普风 / 商务风 / 电影风等（v0.2）
- 图像约束：16:9 横版、主体占画面 40–60%、留白 ≥35%、手写标注 5–8 个、不写标题、不做正式图表
- 图片落盘到 `data/images/<projectId>/<shotId>.png`，通过 API 路由提供服务
- 失败可单独重试，互不影响

### 4.4 旁白生成（兼作后期字幕）

**输入**：分镜列表（主题 + 主角动作 + 标注词）+ 文章摘要
**输出**：每镜头一条 6–8 秒口播稿（约 35–55 汉字）+ 整体配音节奏提示；旁白文案同时就是后期字幕的内容来源

**人设（写入系统提示词）**：「行业内行的冷幽默老兵」——不喊不吵，梗藏在事实后面；每段口播是「第一句陈述事实 + 第二句抖梗或反问」的结构，重音落在第二句；语言口语化，念出来不拗口。

**人工修订**：每条旁白可直接编辑。

### 4.5 发布包生成

**输入**：视频主题 + 镜头概要 + 旁白全文
**输出**：

- `titles`：5–6 个标题，每个 ≤20 汉字，标注钩子路线（反差 / 利益数字 / 悬念 / 对仗扎心 / 场景故事 / 痛点反常识）
- `body`：发布正文，<100 字，结尾留互动钩子
- `tags`：三层标签——核心（蹭精准流量）/ 场景（蹭泛流量）/ 情绪（蹭话题池），各 3–4 个
- `comment`：发布后自己置顶的第一条评论，用来引导互动

各字段点击即复制。

### 4.6 设置

- LLM 配置：Base URL / API Key / 模型名（默认 OpenAI `gpt-4o-mini`，兼容任何 OpenAI 兼容服务商）
- 图像配置：Base URL / API Key / 模型名 / 尺寸（默认 `gpt-image-1`，1536×1024）
- 风格 DNA：自由文本，注入所有图像提示词（当前全局唯一；按项目选择 IP/风格在 v0.2）

## 5. 非功能需求

| 维度 | 要求 |
|---|---|
| 部署形态 | 本地单机，localhost 访问，无需登录 |
| 数据存储 | 文件系统（JSON + PNG），零外部数据库，可整体备份 `data/` 目录 |
| 原子性 | 每个生成环节独立调用、独立落盘，单点失败不影响已有产物 |
| 密钥安全 | API Key 仅存本机 `data/settings.json`，`.gitignore` 排除，不入库 |
| 成本可控 | 所有模型调用由用户显式点击触发，无后台自动调用 |

## 6. 技术方案摘要

- **框架**：Next.js 14 App Router + React 18 + TypeScript
- **数据层**：`lib/store.ts` 提供 JSON 文件读写；Project 为核心聚合根，步骤产物（shots / images / voiceover / publish）逐级追加
- **模型调用**：`lib/llm.ts` 封装 OpenAI 兼容 `/chat/completions`；`lib/images.ts` 封装 `/images/generations`（b64_json 与 url 返回双兼容）；`lib/prompts.ts` 集中管理四类提示词模板
- **容错**：LLM 返回的 JSON 做 markdown 围栏剥离 + 首尾截取容错解析

## 7. MVP 边界与路线图

| 阶段 | 内容 | 状态 |
|---|---|---|
| v0.1（MVP） | 项目管理 + 提炼观点/分镜 + 配图（默认小黑 IP）+ 旁白 + 发布包 | ✅ 已完成 |
| v0.2 素材升级 | URL 导入文章；自定义 IP 角色（用户可创建自己的 IP，小黑为缺省）；风格预设（科普风 / 商务风 / 电影风等） | 待启动 |
| v0.3 成片环节 | 旁白导出 SRT 字幕轨；TTS 配音；桥接本地视频合成引擎（图转片段、片段合并成完整视频） | 待启动 |
| v0.4 发布增强 | 发布包多平台变体（抖音 / 视频号 / 小红书 / B 站差异化文案） | 待启动 |

## 8. 成功指标（个人工具口径）

- 单篇素材生产时间从 2–4 小时压缩到 1 小时以内
- 修订成本：AI 初稿的直接可用率 ≥60%（分镜结构、旁白节奏基本不用大改）
- 流程留存：连续生产 5 篇后仍愿意用它，而不是退回纯手工
